# Dockerfile for KeyDB with RedisSearch

This is simply a dockerfile for building KeyDB with the Redis Search Module

## Building Image

Simply run the following:

```bash
docker build -t cfryerdev/keydb-redissearch .
```